# AI Bot - Чат-виджет с AI

Golang приложение для интеграции AI чата на любой веб-сайт. Поддерживает OpenRouter и OpenAI API.

##  Возможности

-  **Один исполняемый файл** - просто скачайте и запустите
-  **Современный TUI конфигуратор** с навигацией стрелками
-  **Перетаскиваемый виджет** - кнопку и окно можно перемещать мышкой
-  **Полная кастомизация** - цвета, стили, системные промпты
-  **Адаптивный дизайн** - работает на мобильных устройствах
-  **Сохранение позиции** - виджет запоминает где его оставили
-  **Гибкая настройка** - через .env, аргументы или JavaScript API
-  **Простая интеграция** - один тег `<script>`
  

##  Быстрый старт

### 1. Скачивание и сборка

```bash
git clone <repository>
cd ai-bot
go build -o ai-bot.exe
```

### 2. Настройка (интерактивно)

```bash
./ai-bot.exe --config
```

Запустится TUI конфигуратор с:
-  **Бесплатными моделями** (Llama, Mistral, Qwen)
-  **Популярными моделями** (Claude, GPT-4, Gemini)
-  **Всеми доступными моделями**
-  **Настройкой системного промпта**

![Model Selection](img/model-selection.png)

### 3. Запуск

```bash
./ai-bot.exe
```

### 4. Интеграция на сайт

```html
<!-- Просто добавьте один тег! -->
<script src="http://localhost:8080/chat.js"></script>
```

## Кастомизация

### Цвета и стили

```html
<script src="http://localhost:8080/chat.js"
        data-primary-color="#e74c3c"
        data-secondary-color="#c0392b"
        data-accent-color="#f39c12"
        data-custom-css=".ai-chat-toggle{border:3px solid gold;}"></script>
```

![Custom Theme](img/custom-theme.png)

### Системный промпт

```html
<script src="http://localhost:8080/chat.js"
        data-system-prompt="Ты дружелюбный помощник программиста. Объясняй код простыми словами и добавляй эмодзи."></script>
```

![Programming Assistant](img/programming-assistant.png)

### Полная кастомизация

```html
<script src="http://localhost:8080/chat.js"
        data-primary-color="#2c3e50"
        data-secondary-color="#34495e"
        data-accent-color="#3498db"
        data-system-prompt="Ты профессиональный консультант. Давай четкие, структурированные ответы."
        data-custom-css=".ai-chat-window{border-radius:0;box-shadow:0 0 20px rgba(0,0,0,0.5);}"></script>
```

## Конфигурация

### Файл .env

```env
# Сервер
HOST=0.0.0.0
PORT=8080

# OpenRouter API (рекомендуется)
OPENROUTER_API_KEY=your_openrouter_key_here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# OpenAI API (альтернатива)
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o

# Параметры AI
MAX_TOKENS=4000
TEMPERATURE=0.3
TIMEOUT=30

# Системный промпт (необязательно)
SYSTEM_PROMPT=Ты полезный AI ассистент. Отвечай кратко и по делу на русском языке.
```

### Аргументы командной строки

```bash
./ai-bot.exe \
  --host=0.0.0.0 \
  --port=8080 \
  --openrouter-key=YOUR_API_KEY \
  --openrouter-model=anthropic/claude-3.5-sonnet \
  --max-tokens=4000 \
  --temperature=0.3 \
  --timeout=30
```

##  Параметры кастомизации

| Параметр | Описание | Пример |
|----------|----------|---------|
| `data-primary-color` | Основной цвет (кнопка, заголовок) | `#e74c3c` |
| `data-secondary-color` | Вторичный цвет (градиенты) | `#c0392b` |
| `data-accent-color` | Цвет акцента (badge, уведомления) | `#f39c12` |
| `data-system-prompt` | Системный промпт для AI | `Ты дружелюбный помощник...` |
| `data-custom-css` | Дополнительные CSS стили | `.ai-chat-toggle{border:2px solid gold;}` |

## 🖱️ Интерактивные возможности

### Перетаскивание
- **Кнопка чата** - зажмите и перетащите в любое место экрана
- **Окно чата** - перетаскивайте за заголовок когда чат открыт
- **Сохранение позиции** - виджет запомнит где вы его оставили

### Управление
- **Клик по кнопке** - открыть/закрыть чат
- **Клик вне окна** - автоматическое закрытие
- **Enter в поле ввода** - отправить сообщение
- **Быстрые кнопки** - популярные запросы

##  API

### POST `/api/chat`

```javascript
const response = await fetch('/api/chat', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        message: "Привет!",
        history: [
            {role: "user", content: "Как дела?"},
            {role: "assistant", content: "Отлично! А у тебя?"}
        ],
        systemPrompt: "Ты дружелюбный помощник" // необязательно
    })
});
```

### GET `/api/status`

```javascript
const status = await fetch('/api/status').then(r => r.json());
// {configured: true, provider: "OpenRouter", available: true}
```

##  Демо страницы

- **`/demo`** - стандартная тема с документацией
- **`/demo-custom`** - красная кастомная тема  
- **`/demo-prompt`** - программист-помощник с кастомным промптом

![Demo Pages](img/demo-pages.png)

##  Примеры системных промптов

### Дружелюбный помощник
```
Ты дружелюбный AI помощник. Общайся тепло и неформально, используй эмодзи. 
Будь позитивным и поддерживающим в любых ситуациях.
```

### Программист-наставник
```
Ты опытный программист-наставник. Помогай с кодом, объясняй концепции простыми словами, 
предлагай лучшие практики. Будь терпеливым с новичками и используй примеры.
```

### Профессиональный консультант
```
Ты профессиональный консультант. Давай четкие, структурированные ответы с примерами. 
Анализируй проблемы системно и предлагай конкретные решения.
```

### Креативный помощник
```
Ты креативный помощник и генератор идей. Помогай с творческими задачами, 
предлагай нестандартные решения, вдохновляй на новые проекты.
```

##  Продвинутое использование

### Кастомная интеграция

```html
<!DOCTYPE html>
<html>
<head>
    <title>Мой сайт</title>
    <style>
        /* Ваши стили */
        .my-custom-chat {
            position: fixed;
            bottom: 100px;
            left: 20px;
        }
    </style>
</head>
<body>
    <h1>Добро пожаловать!</h1>
    
    <!-- AI Bot с кастомной позицией -->
    <script src="http://localhost:8080/chat.js"
            data-primary-color="#9b59b6"
            data-secondary-color="#8e44ad"
            data-system-prompt="Ты помощник интернет-магазина. Помогай с выбором товаров и отвечай на вопросы о доставке."
            data-custom-css=".ai-chat-widget{bottom:100px;left:20px;right:auto;}"></script>
</body>
</html>
```

### Программная интеграция

```javascript
// Отправка сообщения через API
async function askAI(question) {
    const response = await fetch('/api/chat', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            message: question,
            systemPrompt: "Отвечай максимально кратко, одним предложением."
        })
    });
    
    const data = await response.json();
    return data.response;
}

// Использование
const answer = await askAI("Что такое JavaScript?");
console.log(answer);
```

##  Рекомендуемые модели

### Бесплатные (через OpenRouter)
- `meta-llama/llama-3.1-8b-instruct` - быстрая и качественная
- `mistralai/mistral-7b-instruct` - хорошо для кода
- `microsoft/phi-3-medium-4k-instruct` - компактная и умная

### Премиум (лучшее качество)
- `anthropic/claude-3.5-sonnet` - лучший для сложных задач
- `openai/gpt-4o` - универсальный и быстрый
- `google/gemini-pro-1.5` - отлично для анализа

##  Мобильная поддержка

Виджет автоматически адаптируется под мобильные устройства:
- Уменьшенный размер окна чата
- Оптимизированные отступы
- Сенсорное управление перетаскиванием
- Адаптивная типографика

##  Безопасность

- API ключи хранятся только на сервере
- Нет логирования сообщений пользователей
- CORS настроен для безопасной интеграции
- Валидация всех входящих данных

##  Развертывание

### Docker
```dockerfile
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN go build -o ai-bot

FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/ai-bot .
COPY --from=builder /app/static ./static
EXPOSE 8080
CMD ["./ai-bot"]
```

### Systemd Service
```ini
[Unit]
Description=AI Bot Service
After=network.target

[Service]
Type=simple
User=aibot
WorkingDirectory=/opt/ai-bot
ExecStart=/opt/ai-bot/ai-bot
Restart=always

[Install]
WantedBy=multi-user.target
```



##  Лицензия

MIT License - используйте свободно в коммерческих и некоммерческих проектах.



